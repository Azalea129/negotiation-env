# ── Simulation / data collection (lash/) ─────────────────────────────────
openai>=1.30.0

# ── Concordia (Stage 1 multi-game env) — optional ─────────────────────────
# pip install concordia
# google-deepmind/concordia (installs its own LLM backend deps)

# ── Model training (model/) ───────────────────────────────────────────────
torch>=2.3.0
transformers>=4.43.0
peft>=0.12.0
accelerate>=0.33.0
